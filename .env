# Committed on purpose: nothing here is secret, and the nightly workflow runs
# with no repo secrets configured. Bun loads this file automatically.

# Drives the "Uptime" line in the SVGs.
BIRTHDAY=1998-11-04

# Not read by today.ts yet — reserved for when the GitHub stats block comes
# back, which also needs an ACCESS_TOKEN kept in repo secrets (never here).
USER_NAME=glpecile
